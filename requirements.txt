# Core UI framework
# Pinned exactly (not >=): the native scratch-waveform QML plugin
# (player/native/scratch_waveform/) is built against a Qt6 dev kit chosen to
# match PyQt6-Qt6's bundled Qt build (build.py's _find_qt6_cmake_prefix/
# _pyqt6_qt_version) — Qt's QML plugin loader rejects any version mismatch
# outright ("uses incompatible Qt library"), so an unpinned PyQt6 upgrade
# silently breaks the footer panel until the matching Qt6 kit is reinstalled.
PyQt6==6.10.2
PyQt6-Qt6==6.10.2

# Audio metadata (tags, duration, bitrate)
mutagen>=1.46.0

# Album art decoding and blurring
Pillow>=9.0.0
numpy>=1.24.0
scipy>=1.10.0

# HTTP requests (streaming, cover art downloads)
requests>=2.28.0

# Memory / RAM monitoring
psutil>=5.9.0

# OS keyring (secure credential storage)
keyring>=23.0.0

# Media key support on Windows (Linux uses evdev)
pynput>=1.7.6

# Optional: evdev for Linux media key support
# Install manually on Linux: pip install evdev

# Cast / AirPlay support
pychromecast>=14.0.0
async-upnp-client>=0.36.0
aiohttp>=3.9.0
pyatv>=0.14.0
zeroconf>=0.115.0
